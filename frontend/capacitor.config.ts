import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.seuapp.leite', // ✅ Use um ID exclusivo e válido para publicação
  appName: 'LeitePrecoApp',  // ✅ Nome do app visível no dispositivo
  webDir: 'www',
};

export default config;
